show 'Enter size: ';
s_input = read;
size = int(s_input);

if size > 0:
  for i in range(size):
    for j in range(size):
      show '**';
    close
    show '\n';
  close
else:
  show 'Invalid input';
close