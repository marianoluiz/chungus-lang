"""Lexer package.

Modules:
- lexer: orchestrates the DFA traversal and tokenization
- td: defines the DFA state machine (STATES)
- token: simple classifier to convert raw lexemes into typed tokens
- error_handler: error object definitions for friendly diagnostics
"""
# This file treats the directory as a package and imports the modules of the lexer package.
