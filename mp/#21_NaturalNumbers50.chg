fn printNaturals(current, max):
    if (current <= max):
        show current;
        show ' ';         
        nextNum = current + 1;
        printNaturals(nextNum, max);
    close
close

show 'The natural numbers are : ';
printNaturals(1, 50);
show '\n';